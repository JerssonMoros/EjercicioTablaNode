# Notas:
Este es mi primer programa con NODE y JacaScript

```
Options:
      --help     Show help                                             [boolean]
      --version  Show version number                                   [boolean]
  -b, --base     Numero para generar la abla de multiplicar  [number] [required]
  -l, --listar   valor para imprimir o no en consola  [boolean] [default: false]
  -h, --hasta    Hasta que numero se va a multiplicar     [number] [default: 10]


```